export const Name = 'Name';

/**
 * Name component for entities.
 * Used for display purposes (UI, combat messages, etc.)
 */
export interface Name {
  /** Display name for the entity */
  name: string;
}
