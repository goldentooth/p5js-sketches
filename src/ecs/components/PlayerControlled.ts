/**
 * Tag component marking an entity as player-controlled
 * Entities with this component receive input commands
 */
export interface PlayerControlled {
  // Empty for now - future: playerId, inputSource, etc.
}
